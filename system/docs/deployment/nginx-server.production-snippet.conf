# DEPLOYMENT-REFERENCE-NGINX-MARKER-01 — minimal production server fragment; replace root and fastcgi_pass.
# DEPLOYMENT-REFERENCE-NGINX-ELITE-MARKER-01 — root directive MUST end with /system/public (same folder as index.php here).
# root MUST be …/your-clone/system/public (same directory as system/public/index.php).
#
# server {
#     listen 443 ssl http2;
#     server_name app.example.com;
#     root /var/www/spa/system/public;
#     index index.php;
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }
#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_pass unix:/run/php/php-fpm.sock;
#     }
# }
#
# Defense-in-depth if root were ever mis-set to repo parent: deny non-public trees (see main deployment doc).
