# Shared

Reusable UI primitives and presentation components. **No business logic.**

Use only for:
- Generic form controls, table wrappers, filters
- Modals, cards, timelines, calendar views
- Charts, upload components
- Base layout and common shell

Do not place module-specific logic here. Shared components receive data; they do not fetch or transform business data.
