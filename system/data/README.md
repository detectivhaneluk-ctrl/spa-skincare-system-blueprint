# Data

Migrations, seeders, schemas. Database structure and initial data.

## Structure

| Path | Purpose |
|------|---------|
| `migrations/` | Versioned schema changes |
| `seeders/` | Initial and test data |
| `schemas/` | Canonical schema definitions (optional) |
