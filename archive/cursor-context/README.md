# Archival Cursor context exports

**Not authoritative.** These JSON/Markdown files are **snapshots** for Cursor or similar tooling. They are **not** validated on every release and may drift from `system/` code and routes.

For current runtime truth, use:

- `system/docs/MAINTAINER-RUNTIME-TRUTH.md`
- `system/README.md`
